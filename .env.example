DATABASE_URL="file:./dev.db"

AUTH_SECRET="generate-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Comma-separated Google emails allowed to access the admin app.
# Sign-in is denied for any other address (fail-closed if empty).
ADMIN_ALLOWED_EMAILS="owner@example.com"
